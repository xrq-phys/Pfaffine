# Recommanded configuration for the supercompute Fugaku.
# As for May., 2020, compiling on the arm1 login node doest the best job.
AR=ar

# ARM Compiler toolset.
CC=armclang
CFLAGS=-O2 -mcpu=a64fx
CXX=armclang++
CXXFLAGS=-std=c++11 -O3 -mcpu=a64fx
# The code is written thread-aware so that
# one can add -fopenmp flag to enable threaded version.
#CXXFLAGS=-std=c++11 -O3 -fopenmp

# BLIS installation.
BLIS_ROOT=$(HOME)/blis
